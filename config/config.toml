# Node-local proxmox-notify configuration.
#
# `subscribe` preserves this value and writes a matching systemd timer drop-in
# for new subscriptions. The shipped timer defaults to the same interval.
reconcile_interval = "60s"

publishes = []
subscribes = []

[handlers]
