# Node-local proxmox-notify configuration.
#
# The agent polls at this interval. Polling is the correctness path; filesystem
# watches should only ever be treated as optional latency hints.
reconcile_interval = "60s"

publishes = []
subscribes = []

[handlers]
